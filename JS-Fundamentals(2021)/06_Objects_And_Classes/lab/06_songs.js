function solveSongs(songs){
    let n = songs.shift();
    class Songs{
        consturctor(typeList, name, time){
            this.typeList = typeList;
            this.name = name;
            this.time = time;
        }
    }

    for(let i = 0; i < n; i++){
        
    }
}